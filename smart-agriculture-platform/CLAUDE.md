# CLAUDE.md - AI 协作指南

本文档为 Claude（或其他 AI 助手）提供项目协作指引，帮助 AI 高效、规范地参与智慧农业预测平台的开发与维护。

---

## 📚 目录

- [项目概览](#项目概览)
- [代码风格与规范](#代码风格与规范)
- [技术栈与依赖](#技术栈与依赖)
- [项目结构导航](#项目结构导航)
- [常用开发命令](#常用开发命令)
- [测试与质量门槛](#测试与质量门槛)
- [禁止修改的目录](#禁止修改的目录)
- [变更日志规范](#变更日志规范)
- [PR 提交检查清单](#pr-提交检查清单)
- [常见陷阱与注意事项](#常见陷阱与注意事项)
- [高风险操作白名单](#高风险操作白名单)
- [数据处理规范](#数据处理规范)
- [性能优化指引](#性能优化指引)

---

## 🎯 项目概览

### 基本信息

- **项目名称**: 智慧农业预测平台（Smart Agriculture Platform）
- **项目类型**: React 单页应用（SPA）
- **技术栈**: React 19 + TypeScript + Vite + Tailwind CSS + Sass
- **代码规模**: 约 3,263 行（TypeScript 1,590 行 + 样式 1,673 行）
- **当前版本**: v0.0.0（MVP 阶段）
- **当前状态**: 前端模拟数据，未连接真实后端

### 核心功能

1. **农田数据可视化**：温度、湿度、微生物活性、NDVI 四大指标的历史趋势和预测展示
2. **智能建议生成**：基于阈值规则引擎的管理建议输出
3. **数据导出**：CSV 格式报告下载
4. **多农田管理**：支持多地块独立监测

### 目标受众

- 🌾 中小型农场主
- 🎓 农业科研人员
- 📊 农业数据分析师
- 💡 智慧农业解决方案提供商

---

## 🎨 代码风格与规范

### TypeScript 规范

#### 1. 严格类型检查

项目启用了严格模式（`strict: true`），**必须遵守**：

```typescript
// ✅ 正确：清晰的类型定义
interface FieldCardProps {
  field: Field;
}

const FieldCard = ({ field }: FieldCardProps): JSX.Element => {
  // ...
};

// ❌ 错误：使用 any 类型
const processData = (data: any) => { /* ... */ };

// ❌ 错误：隐式 any
const getData = (id) => { /* ... */ };
```

#### 2. 类型定义位置

- **共享类型**：统一放在 `src/types/index.ts`
- **组件私有类型**：定义在组件文件顶部
- **工具函数类型**：紧邻函数定义

```typescript
// src/types/index.ts
export type DataPoint = {
  date: string;
  temperature: number;
  // ...
};

// src/components/FieldCard.tsx
interface FieldCardProps {
  field: Field;
}
```

#### 3. 禁止的类型使用

- ❌ `any`（除非有充分理由并添加注释）
- ❌ `object`（应使用具体接口）
- ❌ `Function`（应使用具体函数签名）

```typescript
// ❌ 避免
const handler: Function = () => {};

// ✅ 推荐
const handler: () => void = () => {};
```

### React 组件规范

#### 1. 组件定义

**优先使用函数组件 + Hooks**：

```typescript
// ✅ 推荐：箭头函数 + 显式类型
const FieldCard = ({ field }: FieldCardProps): JSX.Element => {
  const [loading, setLoading] = useState<boolean>(false);

  return <div>{/* ... */}</div>;
};

// ❌ 避免：类组件（除非必要）
class FieldCard extends React.Component {
  // ...
}
```

#### 2. Props 接口命名

- 组件 Props：`ComponentNameProps`
- 示例：`FieldCardProps`、`LineChartPanelProps`

#### 3. 状态管理

```typescript
// ✅ 正确：明确类型
const [fields, setFields] = useState<Field[]>([]);
const [loading, setLoading] = useState<boolean>(true);

// ❌ 避免：依赖类型推断（当初始值为空时）
const [fields, setFields] = useState([]); // 类型为 never[]
```

#### 4. 事件处理器命名

- **格式**：`handle<动作>` 或 `on<动作>`
- **示例**：`handleExport`、`onClick`

```typescript
// ✅ 推荐
const handleExport = () => {
  downloadCSV(field);
};

<button onClick={handleExport}>导出</button>
```

### 样式规范

#### 1. 三层样式架构

项目采用**混合样式方案**，需严格遵守层级：

| 层级 | 技术 | 使用场景 |
|------|------|---------|
| **全局样式** | `src/index.css` | 主题色、动画关键帧、全局类（.glass-card, .glow-button） |
| **Tailwind 原子类** | `className="..."` | 简单布局、间距、颜色（如 `flex`, `p-4`, `text-primary`） |
| **SCSS 模块** | `*.module.scss` | 复杂组件样式、交互状态、媒体查询 |

#### 2. 样式选择规则

```typescript
// ✅ 简单样式用 Tailwind
<div className="flex items-center gap-4 p-6 bg-dark rounded-xl">

// ✅ 复杂样式用 SCSS 模块
import styles from './FieldCard.module.scss';
<div className={styles.fieldCard}>

// ✅ 混合使用（Tailwind + SCSS）
<div className={`${styles.card} flex items-center`}>
```

#### 3. SCSS 命名规范

- **采用 BEM 变体**（块-元素-修饰符）
- **camelCase** 命名

```scss
// ✅ 推荐
.fieldCard {
  background: rgba(255, 255, 255, 0.05);

  &:hover {
    transform: scale(1.02);
  }
}

.cardBody {
  padding: 1.5rem;
}

// ❌ 避免：kebab-case（与 CSS Modules 导入冲突）
.field-card { /* ... */ }
```

#### 4. 颜色使用

**优先使用 Tailwind 自定义颜色变量**：

```typescript
// ✅ 推荐
<div className="bg-primary text-dark">

// ✅ 或使用 SCSS 变量
// src/styles/_variables.scss
$primary: #00FFC6;
```

**禁止硬编码颜色**（除非一次性使用）：

```scss
// ❌ 避免
.element {
  background: #00FFC6; // 应使用 $primary
}
```

### 命名约定总览

| 类型 | 规范 | 示例 |
|------|------|------|
| **组件** | PascalCase | `FieldCard`, `LineChartPanel` |
| **函数** | camelCase | `generateDataPoint`, `calculateFieldStats` |
| **常量** | UPPER_SNAKE_CASE | `MAX_PREDICTIONS_DAYS` |
| **类型/接口** | PascalCase | `Field`, `DataPoint`, `FieldCardProps` |
| **CSS 类（SCSS）** | camelCase | `.fieldCard`, `.cardBody` |
| **文件名** | PascalCase（组件）/camelCase（工具） | `FieldCard.tsx`, `dataGenerator.ts` |

---

## 🛠️ 技术栈与依赖

### 核心依赖速查

| 依赖 | 版本 | 用途 | 常用 API |
|------|------|------|---------|
| **react** | 19.1.1 | UI 框架 | `useState`, `useEffect`, `useMemo` |
| **react-router-dom** | 7.9.5 | 路由管理 | `useNavigate`, `useParams`, `useLocation` |
| **recharts** | 3.3.0 | 图表库 | `<AreaChart>`, `<Line>`, `<Tooltip>` |
| **papaparse** | 5.5.3 | CSV 处理 | `Papa.unparse()` |
| **@tsparticles/react** | 3.0.0 | 粒子背景 | `<Particles>` |
| **tailwindcss** | 4.1.16 | 样式框架 | utility classes |
| **sass** | 1.93.3 | CSS 预处理 | SCSS 语法 |

### 包管理规则

1. **新增依赖必须先评估**：
   - 是否有现有依赖可以实现？
   - 包的维护状态（最后更新时间、Star 数、Issue 活跃度）
   - 包大小对构建体积的影响
   - 是否有更轻量的替代方案？

2. **安装命令**：

```bash
# 生产依赖
npm install <package-name>

# 开发依赖
npm install -D <package-name>

# 指定版本
npm install <package-name>@<version>
```

3. **依赖更新策略**：
   - 安全补丁：立即更新
   - 次版本更新：每月审查
   - 主版本更新：充分测试后更新

---

## 📂 项目结构导航

### 目录树

```
src/
├── components/          # 可复用 UI 组件
│   ├── ui/             # 基础组件（Card, Button）
│   ├── Header.tsx      # 导航栏
│   ├── Footer.tsx      # 页脚
│   ├── FieldCard.tsx   # 农田卡片
│   ├── LineChartPanel.tsx      # 图表组件
│   ├── RecommendationPanel.tsx # 建议面板
│   ├── ParticleBackground.tsx  # 粒子背景
│   └── Section.tsx     # 章节容器
├── pages/              # 页面组件（对应路由）
│   ├── Home.tsx        # 首页（/）
│   ├── FieldDetail.tsx # 农田详情（/field/:id）
│   └── About.tsx       # 关于页（/about）
├── types/              # TypeScript 类型定义
│   └── index.ts        # 所有共享类型
├── utils/              # 工具函数
│   └── dataGenerator.ts # 数据生成与处理
├── styles/             # 样式资源
│   └── _variables.scss # SCSS 变量
├── assets/             # 静态资源（图片、字体）
├── App.tsx             # 应用根组件（路由配置）
├── main.tsx            # 应用入口
└── index.css           # 全局样式（Tailwind + 自定义）
```

### 文件职责

| 文件 | 职责 | 可修改性 |
|------|------|---------|
| **src/types/index.ts** | 全局类型定义 | ✅ 可扩展（添加新类型） |
| **src/utils/dataGenerator.ts** | 数据生成逻辑 | ⚠️ 谨慎修改（影响全局数据） |
| **src/App.tsx** | 路由配置 | ✅ 添加新路由 |
| **src/components/ui/** | 基础 UI 组件 | ⚠️ 修改影响所有使用处 |
| **src/index.css** | 全局样式 | ⚠️ 修改影响全局主题 |
| **tailwind.config.js** | Tailwind 配置 | ⚠️ 修改影响所有样式 |
| **vite.config.ts** | 构建配置 | 🔒 除非必要否则不动 |
| **tsconfig.*.json** | TypeScript 配置 | 🔒 除非必要否则不动 |

### 添加新组件的标准位置

```bash
# 可复用 UI 组件
src/components/ComponentName.tsx
src/components/ComponentName.module.scss  # 如需独立样式

# 页面组件
src/pages/PageName.tsx
src/pages/PageName.module.scss

# 业务逻辑
src/utils/utilityName.ts

# 类型定义
src/types/index.ts  # 追加导出
```

---

## ⚙️ 常用开发命令

### 开发流程命令

```bash
# 1. 启动开发服务器（HMR 热重载）
npm run dev
# → 访问 http://localhost:5173

# 2. 代码检查（ESLint）
npm run lint
# 修复可自动修复的问题
npm run lint -- --fix

# 3. 构建生产版本
npm run build
# 输出：dist/ 目录

# 4. 预览构建结果
npm run preview
# → 访问 http://localhost:4173（默认）

# 5. 类型检查（TypeScript）
npx tsc --noEmit
```

### Git 操作命令

```bash
# 查看状态
git status

# 创建功能分支
git checkout -b feature/your-feature-name

# 提交代码
git add .
git commit -m "feat: 描述功能"

# 推送分支
git push origin feature/your-feature-name

# 合并主分支最新代码
git fetch upstream
git merge upstream/master
```

### 依赖管理命令

```bash
# 安装依赖
npm install

# 清理并重新安装
rm -rf node_modules package-lock.json
npm install

# 检查过时依赖
npm outdated

# 更新依赖（谨慎操作）
npm update <package-name>
```

### 调试命令

```bash
# 查看 Vite 配置
npx vite --debug

# 分析构建产物体积
npm run build -- --sourcemap
# 使用 source-map-explorer 分析
npx source-map-explorer dist/assets/*.js
```

---

## 🧪 测试与质量门槛

### 当前状态

⚠️ **项目当前无测试覆盖** - 这是待改进的高优先级事项。

### 代码质量检查清单

在提交 PR 前，**必须通过**以下检查：

```bash
# 1. ESLint 检查（无错误）
npm run lint
# ✅ 必须 0 errors

# 2. TypeScript 编译检查
npx tsc --noEmit
# ✅ 必须无类型错误

# 3. 构建成功
npm run build
# ✅ 必须成功生成 dist/

# 4. 浏览器兼容性测试
# ✅ Chrome、Firefox、Safari 最新版本手动测试
```

### 手动测试检查表

**新功能开发后必须验证**：

- [ ] 首页加载正常（3 个农田卡片显示）
- [ ] 点击卡片进入详情页
- [ ] 详情页图表渲染正常（4 个图表）
- [ ] 详情页建议面板显示
- [ ] 导出 CSV 功能正常
- [ ] 响应式布局（移动端/平板/桌面）
- [ ] 粒子背景动画流畅（无卡顿）
- [ ] 路由导航正常（前进/后退/刷新）
- [ ] 浏览器控制台无错误

---

## 🔒 禁止修改的目录

### 严格禁止修改

以下文件/目录**仅在有明确需求时修改**，修改前**必须询问用户**：

```
🔒 vite.config.ts           # Vite 构建配置
🔒 tsconfig.json            # TypeScript 根配置
🔒 tsconfig.app.json        # TypeScript 应用配置
🔒 tsconfig.node.json       # TypeScript Node 配置
🔒 eslint.config.js         # ESLint 规则
🔒 package.json             # 依赖列表（除非添加新依赖）
🔒 package-lock.json        # 依赖锁定文件（由 npm 管理）
🔒 .gitignore              # Git 忽略规则
```

### 谨慎修改

以下文件修改影响全局，需**充分理由 + 用户确认**：

```
⚠️ src/index.css           # 全局样式和主题
⚠️ tailwind.config.js      # Tailwind 主题配置
⚠️ src/styles/_variables.scss  # SCSS 全局变量
⚠️ src/types/index.ts      # 修改现有类型定义
⚠️ src/utils/dataGenerator.ts # 数据生成逻辑
```

### 自由修改

以下可放心修改（仍需符合代码规范）：

```
✅ src/pages/*.tsx         # 页面组件
✅ src/components/*.tsx    # 新增/修改组件
✅ src/components/ui/*.tsx # UI 组件（但注意影响范围）
✅ README.md / CLAUDE.md   # 文档
```

---

## 📝 变更日志规范

### Commit Message 格式

**必须遵守 Conventional Commits 规范**：

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 类型（type）定义

| 类型 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat(field): 添加农田删除功能` |
| `fix` | Bug 修复 | `fix(chart): 修复图表数据渲染错误` |
| `docs` | 文档更新 | `docs: 更新 README 安装指南` |
| `style` | 代码格式（不影响运行） | `style: 格式化 FieldCard 组件` |
| `refactor` | 重构 | `refactor(utils): 优化数据生成逻辑` |
| `perf` | 性能优化 | `perf: 减少粒子数量提升性能` |
| `test` | 测试相关 | `test: 添加 dataGenerator 单元测试` |
| `chore` | 构建/工具配置 | `chore: 更新 Vite 到 7.1.8` |

### Scope（范围）建议

- `field` - 农田相关功能
- `chart` - 图表组件
- `ui` - UI 组件
- `utils` - 工具函数
- `style` - 样式
- `docs` - 文档
- `deps` - 依赖更新

### 示例

```bash
# ✅ 良好示例
feat(field): 添加农田编辑功能

- 在 FieldCard 添加编辑按钮
- 实现编辑表单和验证逻辑
- 更新数据生成器支持修改操作

Closes #23

# ✅ 修复示例
fix(chart): 修复预测数据虚线不显示的问题

LineChartPanel 组件中的 strokeDasharray 属性
设置不正确，导致预测数据显示为实线。

Fixes #45

# ❌ 错误示例
update code  # 太模糊
fix bug      # 无具体信息
```

---

## ✅ PR 提交检查清单

### 提交前自检（必须全部通过）

```markdown
## 代码质量
- [ ] ESLint 检查通过（`npm run lint`）
- [ ] TypeScript 编译无错误（`npx tsc --noEmit`）
- [ ] 构建成功（`npm run build`）
- [ ] 无 console.log/debugger 等调试代码残留
- [ ] 代码有适当注释（复杂逻辑必须注释）

## 功能验证
- [ ] 本地测试所有相关功能正常
- [ ] 未破坏现有功能（回归测试）
- [ ] 响应式布局正常（移动端/平板/桌面）
- [ ] 浏览器兼容性测试（Chrome/Firefox/Safari）

## 代码规范
- [ ] 遵守 TypeScript 类型规范（无 any）
- [ ] 遵守命名约定（组件 PascalCase/函数 camelCase）
- [ ] 遵守样式规范（Tailwind + SCSS 分层）
- [ ] Commit Message 符合 Conventional Commits 规范

## 文档
- [ ] 更新相关文档（如有 API/使用方式变更）
- [ ] 添加代码注释（公共 API/复杂逻辑）
- [ ] 更新 README（如有新功能/配置）

## 安全性
- [ ] 无敏感信息泄露（API 密钥、密码等）
- [ ] 输入验证充分（如有用户输入）
- [ ] 依赖无已知安全漏洞（`npm audit`）
```

---

## ⚠️ 常见陷阱与注意事项

### 1. TypeScript 陷阱

#### 陷阱：依赖类型推断导致 never[]

```typescript
// ❌ 错误：初始值为空数组，推断为 never[]
const [fields, setFields] = useState([]);

// ✅ 正确：显式指定类型
const [fields, setFields] = useState<Field[]>([]);
```

#### 陷阱：可选链与空值处理

```typescript
// ❌ 可能导致运行时错误
const name = field.name.toUpperCase();

// ✅ 正确：使用可选链
const name = field?.name?.toUpperCase() ?? '未命名';
```

### 2. React Hooks 陷阱

#### 陷阱：依赖数组缺失

```typescript
// ❌ 错误：缺少依赖 id，导致数据不更新
useEffect(() => {
  fetchFieldData(id);
}, []);

// ✅ 正确：包含所有依赖
useEffect(() => {
  fetchFieldData(id);
}, [id]);
```

#### 陷阱：无限循环

```typescript
// ❌ 错误：每次渲染都创建新对象，导致无限循环
const [filters, setFilters] = useState({});
useEffect(() => {
  fetchData(filters);
}, [filters]); // filters 每次都是新对象

// ✅ 正确：使用 useMemo 或将对象提取到组件外
const initialFilters = {};
const [filters, setFilters] = useState(initialFilters);
```

### 3. 样式陷阱

#### 陷阱：Tailwind 类名冲突

```typescript
// ❌ 错误：后面的类名不会覆盖前面的
<div className="p-4 p-6"> // 最终 padding: 1rem（p-4）

// ✅ 正确：使用条件类名
<div className={isLarge ? "p-6" : "p-4"}>
```

#### 陷阱：SCSS 模块命名冲突

```scss
// ❌ 错误：kebab-case 在 CSS Modules 中导入困难
.field-card { /* ... */ }

// ✅ 正确：camelCase
.fieldCard { /* ... */ }
```

### 4. 路由陷阱

#### 陷阱：路由参数类型未检查

```typescript
// ❌ 错误：id 可能为 undefined
const { id } = useParams();
const field = fields.find(f => f.id === id);

// ✅ 正确：检查并提供默认值
const { id } = useParams<{ id: string }>();
if (!id) return <div>无效的农田 ID</div>;
```

### 5. 性能陷阱

#### 陷阱：粒子数量过多

```typescript
// ❌ 错误：粒子数量过多导致卡顿
particles: {
  number: { value: 200 } // 太多
}

// ✅ 正确：控制在 80 以内
particles: {
  number: { value: 80 }
}
```

---

## 🚨 高风险操作白名单

以下操作**必须先询问用户**，获得明确许可后才能执行：

### 1. 依赖管理

```bash
# ⛔ 禁止自行执行
npm install <新依赖>          # 增加项目体积和维护成本
npm update <依赖>             # 可能引入破坏性变更
npm uninstall <依赖>          # 可能影响现有功能
```

**询问模板**：
> "我计划添加 `<package-name>` 依赖以实现 `<功能>`。该包大小约 `<size>`，最后更新于 `<date>`。是否批准？"

### 2. 配置文件修改

```bash
# ⛔ 禁止自行修改
vite.config.ts         # 影响构建流程
tsconfig.*.json        # 影响类型检查
eslint.config.js       # 影响代码规范
tailwind.config.js     # 影响全局样式
```

**询问模板**：
> "为了实现 `<功能>`，需要修改 `<配置文件>` 的 `<具体配置项>`。这将影响 `<影响范围>`。是否批准？"

### 3. 全局样式/类型修改

```typescript
// ⛔ 禁止自行修改
src/types/index.ts        // 修改现有类型定义
src/index.css             # 全局样式
src/styles/_variables.scss // 全局变量
```

**询问模板**：
> "我计划修改 `<类型/样式>` 的 `<具体内容>`。这将影响以下文件：`<列出影响范围>`。是否批准？"

### 4. 数据逻辑重构

```typescript
// ⛔ 禁止自行重构
src/utils/dataGenerator.ts  // 数据生成核心逻辑
```

**询问模板**：
> "我发现 `<函数名>` 存在 `<问题>`，计划 `<重构方案>`。这将影响所有使用该函数的地方。是否批准？"

### 5. Git 操作

```bash
# ⛔ 禁止自行执行
git push --force             # 强制推送（破坏历史）
git rebase -i master         # 交互式变基
git merge master             # 合并主分支
git reset --hard             # 硬重置
```

**询问模板**：
> "为了 `<目的>`，需要执行 `<Git 命令>`。这可能影响 `<影响范围>`。是否批准？"

---

## 📊 数据处理规范

### 1. 数据生成函数使用

```typescript
// src/utils/dataGenerator.ts

// ✅ 生成单个农田数据
const field = generateFieldData(
  'field-1',
  '阳光示范田',
  '北纬 35.2° 东经 108.9°',
  '优质小麦',
  120,
  '...',
  123  // seed（相同 seed 生成相同数据）
);

// ✅ 生成所有农田数据
const fields = generateAllFieldsData();

// ✅ 计算统计指标（基于最近 7 天）
const stats = calculateFieldStats(field);

// ✅ 生成智能建议
const recommendations = generateRecommendations(stats);

// ✅ 导出 CSV
downloadCSV(field);
```

### 2. 数据验证规则

**添加数据验证函数时必须检查**：

```typescript
// ✅ 温度范围
const isValidTemperature = (temp: number) => temp >= 15 && temp <= 35;

// ✅ 湿度范围
const isValidHumidity = (humidity: number) => humidity >= 30 && humidity <= 90;

// ✅ 微生物活性范围
const isValidMicrobialActivity = (activity: number) => activity >= 0.2 && activity <= 1.0;

// ✅ NDVI 范围
const isValidNdvi = (ndvi: number) => ndvi >= 0.3 && ndvi <= 1.0;
```

---

## ⚡ 性能优化指引

### 1. 组件性能优化

```typescript
// ✅ 使用 useMemo 缓存计算结果
const stats = useMemo(() => calculateFieldStats(field), [field]);

// ✅ 使用 useCallback 缓存函数引用
const handleExport = useCallback(() => {
  downloadCSV(field);
}, [field]);

// ✅ 使用 React.memo 避免不必要的重渲染
export const FieldCard = React.memo(({ field }: FieldCardProps) => {
  // ...
});
```

### 2. 图表性能优化

```typescript
// ⚠️ 注意：Recharts 在大数据量下性能较差
// 数据点 > 500 时，考虑采样

const optimizedData = useMemo(() => {
  if (data.length > 500) {
    return sampleData(data, 300); // 采样到 300 个点
  }
  return data;
}, [data]);
```

### 3. 粒子背景优化

```typescript
// ✅ 控制粒子数量（推荐 50-100）
particles: {
  number: { value: 80 }
}

// ✅ 限制 FPS（推荐 60）
fpsLimit: 60

// ✅ 移动端减少粒子
const particleCount = window.innerWidth < 768 ? 30 : 80;
```

---

## 🎓 生成内容质量门槛

### 代码生成标准

当 AI 生成代码时，**必须满足**：

1. **类型安全**：
   - ✅ 所有变量/参数有明确类型
   - ✅ 无 `any` 类型（除非有注释说明原因）
   - ✅ Props 接口完整定义

2. **错误处理**：
   - ✅ 异步操作有 try-catch
   - ✅ 边界情况有检查（null、undefined、空数组）
   - ✅ 用户可感知的错误有友好提示

3. **可读性**：
   - ✅ 复杂逻辑有注释
   - ✅ 函数单一职责（不超过 50 行）
   - ✅ 变量命名语义化

4. **性能**：
   - ✅ 避免不必要的重渲染
   - ✅ 大数据量有优化（虚拟化/采样）
   - ✅ 使用 memo/useMemo/useCallback（适当时）

### 自检流程

生成代码后，**必须自检**：

```markdown
## 代码自检
- [ ] TypeScript 编译无错误
- [ ] ESLint 检查无警告
- [ ] 逻辑完整（无 TODO/FIXME 残留）
- [ ] 性能考虑（无明显性能问题）
```

---

## 📞 需要帮助时

如果遇到以下情况，**立即停止并询问用户**：

1. ❓ **不确定实现方式**：提供 2-3 个方案供用户选择
2. ⚠️ **需要修改高风险文件**：说明影响范围并征求同意
3. 🐛 **发现潜在 Bug**：报告问题而非默默修复
4. 📦 **需要新依赖**：提供依赖信息并等待批准
5. 🔄 **大规模重构**：先提供重构计划并获得批准

---

**本文档最后更新：2025-11-10**
